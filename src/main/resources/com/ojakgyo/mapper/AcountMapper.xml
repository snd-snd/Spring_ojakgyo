<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ojakgyo.mapper.AcountMapper">
	
	<!-- 회원가입 -->
	<insert id="MemberInsert">
		insert into members values(#{userId},#{userPw},#{userName},#{nickName},#{email})
	</insert>
	
	<!-- 로그인 -->
	<select id="MemberLogin" resultType="com.ojakgyo.domain.MemberVO">
		select * from members where userId=#{userId} and userPw=#{userPw}
	</select>
	
	<!-- 아이디 중복검사 -->
	<select id="MemberIdCheck" resultType="com.ojakgyo.domain.MemberVO">
		select * from member where userId=#{userId}
	</select>
	
	<!-- 회원수정 -->
	<update id="MemberUpdate">
		update members set nickName=#{nickName} userPw=#{new_password} email=#{email} where userId=#{userId} and userPw=#{userPw}
	</update>
	
	<!-- 회원탈퇴 -->
	<delete id="MemberDelete">
		delete from members where userId=#{userId} and userPw=#{userPw}
	</delete>
	
</mapper>